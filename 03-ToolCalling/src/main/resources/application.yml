server:
  port: 18081

spring:
  application:
    name: 03-ToolCalling
  config:
    import: optional:classpath:application-local.yml

agentscope:
  model:
    provider: dashscope
  dashscope:
    model-name: qwen-plus
    stream: true

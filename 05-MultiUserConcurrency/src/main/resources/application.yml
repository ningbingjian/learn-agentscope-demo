server:
  port: 18081

spring:
  application:
    name: 05-MultiUserConcurrency

agentscope:
  model:
    provider: dashscope
  dashscope:
    api-key: ${DASHSCOPE_API_KEY}
    model-name: qwen-plus
    stream: true

server:
  port: 18081

spring:
  application:
    name: 05-MultiUserConcurrency
  config:
    import: optional:classpath:application-local.yml

agentscope:
  model:
    provider: dashscope
  dashscope:
    model-name: qwen-plus
    stream: true

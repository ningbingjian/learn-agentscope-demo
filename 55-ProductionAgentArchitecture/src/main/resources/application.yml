server:
  port: 18081
  shutdown: graceful

spring:
  application:
    name: 55-production-agent-architecture
  lifecycle:
    timeout-per-shutdown-phase: 30s

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never

info:
  lesson:
    number: 55
    topic: production-agent-architecture

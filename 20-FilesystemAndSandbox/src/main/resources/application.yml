server:
  port: 18081

spring:
  application:
    name: 20-FilesystemAndSandbox

agentscope:
  dashscope:
    api-key: ${DASHSCOPE_API_KEY:}
    model-name: qwen-plus

demo:
  sandbox:
    enabled: ${SANDBOX_DEMO_ENABLED:false}
    image: ${SANDBOX_IMAGE:python:3.12-slim}
